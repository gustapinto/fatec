import { GithubAPIRepositoryFetcher } from '@sr'

describe('Unit testing for the Github API Fetcher', (): void => {
    it('Test getAllRepositories must not return a empty Array', async (): Promise<void> => {
        //
    })
})